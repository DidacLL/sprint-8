export default class PilotService {
    public static getNextPilots(page:number):any{
        //todo call to api and obtain main Pilot list
    }
    public static getPilot(id:number):any{
        //todo, return all data from 1 Pilot
    }
    public static getAllPilots(page:number):any{
        //todo call to api and obtain main Pilot list
    }
}