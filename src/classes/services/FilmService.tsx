export default class FilmService {
    public static getNextFilms(page:number):any{
        //todo call to api and obtain main Film list
    }
    public static getFilm(id:number):any{
        //todo, return all data from 1 Film
    }
    public static getAllFilms(page:number):any{
        //todo call to api and obtain main Film list
    }
}